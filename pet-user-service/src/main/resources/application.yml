server:
  port: 8081
spring:
  profiles:
    active: dev
# 在配置中心： 可以通过profile进行设置
# 只有默认的配置文件才能结合profile结合使用
# 对应的Dataid: ${spring.application.name}-${profile}.${file-extension:properties}
# profile的后缀必须跟随默认配置文件的格式来


#management:
#  endpoints:
#    web:
#      exposure:
#        include: health,info
